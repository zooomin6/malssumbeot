# DECISIONS.md — 말씀벗 결정 기록

> 한 번 기록된 결정은 에이전트가 임의로 번복하지 않는다.
> 번복이 필요하면 "재검토 요청" 섹션에 사유를 적고 사람 승인을 기다린다.

## 확정된 결정

### D-001. 성경 역본: 개역한글 (1961)
- 날짜: 2026-06-12
- 이유: 저작권 만료로 상업 서비스에 자유롭게 사용 가능. 개역개정은 대한성서공회
  라이선스 필요 → 수익화 검증 후 협의.
- 영향: 성경 DB, 모든 구절 인용

### D-002. MVP 플랫폼: 안드로이드 + iOS 네이티브 앱 (React Native) ★개정
- 날짜: 2026-06-12 (당일 개정, 민규 승인)
- 변경 전: 카카오톡 채널 챗봇 → 변경 후: RN(Expo) 기반 크로스플랫폼 앱
- 이유: 푸시 알림·감사일기·기도제목 등 핵심 경험을 처음부터 앱으로 제공,
  구독 결제 구조에 유리. 검증 기간이 2~3개월로 늘어나는 트레이드오프는 수용.
- 영향: webhook 모듈 → REST API + 인증(소셜 로그인) + 푸시(FCM/APNs)로 대체.
  의도 분류·성경 RAG·위기 필터·프롬프트는 전부 재사용.

### D-007. 앱 기술 스택: React Native (Expo) 단일 코드베이스
- 날짜: 2026-06-12
- 이유: 안드로이드/iOS 동시 출시, 채팅 UI 생태계 성숙, 협업 가능한 RN 경험자 존재.
- 영향: 모바일 레포 분리 (backend / mobile), Expo 빌드 파이프라인

### D-008. 자유 대화 기능 추가 — "신앙 중심 + 일상 동행" 하이브리드
- 날짜: 2026-06-12
- 이유: ChatGPT와 범용성으로 경쟁하지 않음. 일상 대화는 친구처럼 받아주되
  말씀을 강요하지 않고, 코딩·숙제 등 범위 밖 요청은 정중히 안내.
- 영향: 의도 분류에 "일상 대화"·"범위 밖" 카테고리, 모델 라우팅(일상=경량 모델)

### D-003. 성경 구절은 모델 기억이 아닌 DB 원문만 사용
- 날짜: 2026-06-12
- 이유: 구절 환각은 이 서비스의 치명적 리스크. 모델은 구절 주소만 제안하고
  서버가 원문을 조회·삽입한다.
- 영향: BibleVerseService 파이프라인, theology-checker C1 기준

### D-004. 위기 프로토콜은 모든 로직에 우선
- 날짜: 2026-06-12
- 이유: 힘든 사용자가 많이 유입될 서비스 특성상 안전이 신학·기능보다 우선.
- 영향: CrisisFilter를 의도 분류 앞단에 배치, 우회 불가 설계

### D-005. 만드는 에이전트와 검사하는 에이전트 분리
- 날짜: 2026-06-12
- 이유: 자기 산출물 자기 검증은 신뢰 불가. 신학 검사는 독립 에이전트가 비판적으로 수행.
- 영향: agents/theology-checker.md, 머지 차단 규칙

### D-006. 프롬프트·신학 기준 변경은 사람 승인 필수
- 날짜: 2026-06-12
- 이유: 루프 자동화 범위에서 신학적 판단을 제외. 에이전트는 제안까지만.
- 영향: CLAUDE.md "사람 승인 필요 항목"

### D-009. 백엔드 빌드 도구: Maven (wrapper), Spring Boot 3.5.15
- 날짜: 2026-06-12
- 이유: 개발 머신에 Gradle/Maven 미설치 + 로컬 JDK 25 환경에서 구버전 Gradle 호환 리스크
  → Maven wrapper가 JDK 버전에 무관하게 안정적. Boot 버전은 헌법 명세(3.x)의 최신 패치
  3.5.15로 고정 (Initializr 기본값 4.x는 헌법 위반이라 배제).
- 영향: backend/pom.xml, mvnw 기반 빌드·테스트 파이프라인

### D-010. 의도 분류 모델: claude-haiku-4-5 (경량)
- 날짜: 2026-06-12
- 이유: PRD §6.2 단위 경제성("Haiku/Sonnet 혼용")과 모델 라우팅 방침(일상·분류=경량)을 따름.
  분류는 단일 라벨 출력이라 경량 모델로 충분. 응답 생성용 상위 모델은 모델 라우팅
  작업(다음 작업 #4)에서 확정한다.
- 영향: IntentClassifier, application.yml `malssumbeot.anthropic.classifier-model`

### D-011. 위기 라벨은 부분 일치 우선 파싱 (위기 강등 방지)
- 날짜: 2026-06-12
- 이유: theology-checker 판정(2026-06-12, critical) — 모델이 위기를 감지하고도 출력
  형식을 어기면(설명 덧붙임 등) 정확 일치 파싱에 실패해 폴백("상담")으로 강등되는
  경로가 절대 원칙 5(위기 분기 우회 금지)에 저촉. 분류기 출력에 "위기"가 포함되면
  형식과 무관하게 CRISIS로 처리한다.
- 영향: IntentClassifier.classify, D-004 강화. 해석 불가 출력의 폴백은 상담 유지
  (시스템 프롬프트 5종 탑재 시 모든 모드에 위기 프로토콜 내장으로 추가 완화 예정)

### D-012. 위기 감지 1차는 결정론적 패턴 + 세션 sticky 상태
- 날짜: 2026-06-12
- 이유: LLM 분류는 장애·형식 위반·비결정성 리스크가 있어 위기 감지의 단독 수단이
  될 수 없다. 1차는 정규식 패턴(공백 정규화 후 부분 매칭, 재현율 우선), LLM 분류는
  2차 방어선. theology-checker 지적(멀티턴 위기 컨텍스트 휘발)을 반영해 위기 감지 후
  30분(설정값)간 세션을 위기 상태로 유지한다(sticky). 패턴 목록과 유지 시간은
  사람 검토 대상 (PROGRESS "사람 확인 필요").
- 영향: com.malssumbeot.crisis 패키지, crisis-patterns.txt, REST API 인터셉터 배선(예정)

### D-013. 모델 라우팅: 신앙=Sonnet, 일상·범위밖=Haiku
- 날짜: 2026-06-12
- 이유: PRD §6.2 단위 경제성(Haiku/Sonnet 혼용). 신앙 상담·지식QA·기도문·위기는 품질이
  중요해 상위 모델(claude-sonnet-4-6), 일상 대화·범위 밖 안내는 경량 모델(claude-haiku-4-5).
  분류기는 D-010대로 Haiku 유지.
- 영향: ModelRouter, application.yml(faith-model/casual-model), 토큰 비용 통제

### D-014. 환각 구절은 본문에서 제거 후 전송 (재생성으로도 안 잡히면)
- 날짜: 2026-06-12
- 이유: theology-checker 2회차 판정 — 재생성 후에도 미검증(존재하지 않는) 구절 주소가
  응답 본문에 남으면 '그대로 전송'하는 것이 절대 원칙 2 및 T8 '거부' 기준 위반.
  인용 블록 미첨부만으로는 프로즈에 박힌 환각 주소 노출을 막지 못함.
- 결정: 재생성 후에도 미검증 주소가 남으면 그 주소가 든 문장을 본문에서 제거
  (stripSentencesContaining). 제거 후 본문이 비면 결정론적 안전 문구로 대체.
  미검증 주소는 사용자 본문에서 사라지고 모니터링용 목록(unverifiedReferences)에만 남는다.
- 영향: ChatOrchestrator.generate. 알려진 한계(장 단위 인용 미스캔)는 다음 작업 #3로 분리.

### D-015. CLAUDE.md 규칙을 2층으로 구별 + 개발 작업 규칙 추가
- 날짜: 2026-06-27 (민규 지시)
- 이유: 헌법이 "제품 규칙(말씀벗이 응답할 때)"과 "개발 작업 규칙(코딩할 때)"을
  섞고 있었음. 민규가 둘을 구별하고, LLM 코딩 실수를 줄이는 행동 양식 4종
  (코딩 전 생각 / 단순함 우선 / 외과수술적 변경 / 목표 주도 실행)을 제공.
- 결정: 절대 원칙(제품 규칙)은 그대로 두고, "개발 작업 규칙" 섹션을 CLAUDE.md에
  신설. 로그인 등 신앙과 무관한 기능 구현에도 이 섹션이 적용된다.
- 영향: CLAUDE.md "개발 작업 규칙" 섹션. 향후 모든 코딩 작업의 행동 기준.

### D-016. 개역한글 텍스트 소스: 대한성서공회 공식 성경읽기 페이지
- 날짜: 2026-07-02
- 이유: 후보 3곳(holybible.or.kr, 위키문헌, 대한성서공회 자체 성경읽기) 검토.
  holybible.or.kr은 자체 이용안내에 "모바일앱 연동 금지" 명시로 배제. 위키문헌은 본문(PD)과
  별개로 페이지 자체에 CC BY-SA가 덮여있고 자원봉사 전사라 오탈자 위험. 대한성서공회
  자체 페이지(`bskorea.or.kr/bible/korbibReadpage.php?version=HAN`)는 저작권자 본인의 공식
  디지털판이라 판본·정확성 리스크가 가장 낮음. 대한성서공회 자체 저작권 FAQ로 개역한글
  (1961.7.10 발행) 재산권은 2011.12.31 만료(D-001 재확인)이나, 성명표시권·동일성유지권은
  인격권으로 영구 존속함을 확인.
- 결정: `BibleTextScraper`(신규, Spring/DB 비의존 독립 실행 도구, Jsoup)로 해당 페이지를
  긁어 TSV 생성 → 기존 `BibleTextImporter`(수정 없음)로 적재. 각주(`a.comment`/`div.D2`)·
  장 소제목(`font.smallTitle`/`font.chapNum`)은 파싱 시 제거. "18-19" 같은 합쳐진 절 표기는
  각 절 번호 모두에 동일 본문을 매핑(범위 내 절 하나라도 누락되면 정상 구절이 환각으로
  오판될 위험, T8 기준과 충돌).
- 영향: `BibleTextScraper.java`(신규), `BibleBookCatalog`에 영문 코드(`code`) 기반 해석
  추가(TSV에 한글명 대신 코드 사용 — 사이트 코드와 우리 DB 코드가 다른 유일한 사례인 요나는
  siteCode=jnh/dbCode=jon으로 분리 관리). 성명표시권 이행(RN 앱에 "성경전서 개역한글판,
  대한성서공회" 표기)은 PROGRESS.md "사람 확인 필요"로 이관 — 문구·위치는 RN 작업 시 확정.

### D-017. 성경 주소가 든 모델 문장은 전송하지 않고 DB 원문만 별도 전달
- 날짜: 2026-07-12
- 이유: 장·절 주소가 실제로 존재하더라도 모델이 그 옆에 만든 성경 본문·풀이까지 정확하다는
  보장은 없다. theology-checker가 장 단위 인용(`시편 23편`)을 유효 처리하면 모델이 만든 본문이
  그대로 사용자에게 닿는 경로를 C1 FAIL로 지적했다.
- 결정: `ChatOrchestrator`는 모델 응답에서 성경 주소를 하나라도 스캔하면 모델 텍스트 전체를
  전송하지 않는다. 주소와 모델 본문이 서로 다른 문장에 있을 수 있어 문장 단위 제거만으로는
  안전하지 않다. 장·절 주소는 `BibleVerseService`가 조회한 `VersePassage` DB 원문만 별도 응답
  필드로 전달한다. 영어 장절 표기도 DB 책 이름으로 해석하지 못해도 환각 후보로 감지해 제거한다.
  위기 응답은 모델 호출 없이 고정 연락처 안내를 반환해, 성경 주소 차단이 109·1577-0199 안내를
  지우는 경로를 없앤다.
  장 단위 주소는 특정 절 원문을 정할 수 없으므로 모델 문장에서 제거하며, DB 기반 장 인용 UI는
  별도 설계 전까지 제공하지 않는다.
- 영향: ChatOrchestrator.generate/verify, ChatReply.passages. 모델이 생성한 성경 본문이 사용자
  화면에 도달하는 경로를 차단. 장 단위 인용은 주소 검증만 하며 인용 블록을 만들지 않음.

### D-018. 위기 프로토콜 재설계: 사랑 대전제 + 응답 완화(감지 유지) + 학대 분기
- 날짜: 2026-07-14 (민규 논의·승인)
- 배경: 관용적 "죽고싶다" 푸념에도 위기 감지가 발동해 상담전화가 대뜸 나가는 UX 문제 제기.
- 결정:
  1. 감지는 재현율 우선 그대로 유지(D-004/D-012). 감지 완화(미탐 위험)는 채택하지 않음.
     대신 위기 응답 문구를 공감 우선으로 완화한다(결정 A).
  2. "사랑이 기독교 신앙의 뿌리"를 대전제로 명시(master.txt 최상단 + CLAUDE.md 절대원칙 머리).
     사랑은 원칙들이 지키는 목표이며, 사랑을 이유로 원칙(위기 안전·성경 근거·정체성)을 어길 수 없다.
  3. 위기 응답을 카테고리별로 분기: 확실한 자살·자해 신호에만 '곁에 믿을 만한 사람' 권유를 넣고,
     학대·카테고리 불명확·2차(LLM) 판정에는 넣지 않는다(곁의 사람이 가해자일 수 있음).
     `CRISIS_SELF_HARM_TEXT` / `CRISIS_DEFAULT_TEXT`로 분리.
  4. 의도 분류 프롬프트 보강: 제3자 위기·오타/음성 변형은 위기, 타인 분노 비유는 위기 아님,
     타인 죽음 애도는 상담, 질병 치유 여부 질문은 기도문 아님(T4).
- 미결(파생, 법률 검토): 학대 전용 문구·기관 번호(1366·아동보호전문기관 등)·미성년자 진술 데이터
  보관 정책. 현재 학대 응답은 112 안내 안전 잠정본 (PROGRESS "사람 확인 필요").
- 영향: ChatOrchestrator(crisisReply 카테고리 분기), intent-classifier.txt, master.txt, CLAUDE.md.
  독립 신학 검사 PASS(조건부, severity high — 학대 경로는 사람 리뷰 전 배포 불가). 테스트 78건 통과.

### D-019. 위기 응답 세분화: 제3자·학대 분기 + 미탐 패턴 보강
- 날짜: 2026-07-14 (민규 논의·승인; 외부 리뷰(GPT)·독립 신학 검사 교차 확인 반영)
- 배경: 위기 응답이 자살자해·기본 이분이라 (1) 제3자 걱정("친구가 죽고 싶대요")에 1인칭 위로가
  나가고, (2) 학대에 자살예방 번호만 나가며, (3) 결정론 패턴 "때리"가 "때려요"를 놓치는 문제.
- 결정:
  1. 위기 응답을 카테고리별로 세분화: 제3자(CRISIS_THIRD_PARTY_TEXT)·학대(CRISIS_ABUSE_TEXT)·
     자살자해 본인(CRISIS_SELF_HARM_TEXT)·불명확(CRISIS_DEFAULT_TEXT). '믿을 만한 사람' 권유는
     자살자해 본인일 때만 나간다.
  2. crisis-patterns.txt에 제3자위기 패턴을 일반 자살자해 패턴보다 앞에 배치. "때리"를 때(리|려|렸|린)로
     보강(미탐 감소).
  3. intent-classifier 규칙 3 정리: 분노·게임·비유는 위기 아님, 본인/학대/제3자 실제 위기는 위기.
  4. 학대 문구는 1366(여성·가정폭력)·1388(청소년)·112·119 + "네 잘못이 아니야"; '기록이 증거' 같은
     약속·데이터 보관 언급 없음. 번호·데이터 정책은 여전히 법률 검토 대상(PROGRESS "사람 확인 필요").
- 다음 작업(후속): (a) 애매한 신호는 LLM 2차 판정으로 넘기는 2단계 감지(강한 신호는 결정론 유지,
  실패 시 안전 강등 금지) — 정신건강 전문가 검토 필요, (b) sticky에 카테고리 보존,
  (c) CrisisCategory/CrisisSubject enum으로 감지 계층 구조화.
- 영향: ChatOrchestrator(crisisText 라우팅 + 문구 2종), crisis-patterns.txt, intent-classifier.txt,
  테스트 추가. 위기 응답 문구·감지 패턴은 사람 승인 항목.

### D-020. 위기 sticky 단계적 하강(Model 2) + Phase 0 프롬프트 5건 승인
- 날짜: 2026-07-15 (민규 논의·승인)
- 배경: 기존 sticky는 30분 이분법(위기 유지 후 뚝 끊김). 민규가 "30분 뒤 갑자기 괜찮아졌다고 보는 것"보다
  대화를 이어가며 위기 강도를 서서히 낮추길 원함.
- 결정:
  1. `CrisisLevel`(NONE/LOW/MID/HIGH) 도입. 위기 신호 감지 → HIGH. 새 신호 없이 유지 시간
     (`sticky-duration`, 기본 30분)마다 한 단계씩 하강(HIGH→MID→LOW→해제, 총 90분). **새 위기 신호가
     오면 즉시 HIGH로 복귀**(안전 우선). 시간 기반으로만 하강한다 — 사용자가 "괜찮아요"라고 말한다고
     레벨을 낮추지 않는다(진짜 위기 미탐 방지).
  2. 응답 라우팅(Model 2): HIGH=카테고리별 전체 위기 문구(연락처 포함, D-019 그대로),
     MID=`CRISIS_MID_TEXT`, LOW=`CRISIS_LOW_TEXT`(연락처를 매번 반복하지 않고 곁을 지키는 톤,
     "도움 청할 곳" soft 안내 유지). 신규 결정론적 문구 2종.
  3. Phase 0 프롬프트 5건 승인: daily-chat/out-of-scope 본문, 위기 escape hatch 1줄(두 모드),
     일상대화 T2 회복 규칙(눅 15장 환대), 기도문 T7 로또·기복/도구적 요청 경계(이면의 마음을
     하나님 안에서 구하도록 옮김). 환각 폴백 문구·crisis-patterns.txt(수정 없음)·sticky 정책 확인.
- 미결/후속: (a) 애매한 신호는 LLM 2차 판정으로 넘기는 2단계 감지(정신건강 전문가 검토 필요),
  (b) sticky에 카테고리 보존. MID/LOW 문구·시간 하강 설계는 인라인 신학 검사 **조건부 PASS
  (severity high)** — 프로덕션 배포 전 민규 최종 승인 + (권장) 정신건강 전문가 검토.
- 영향: `CrisisLevel`(신규), `CrisisSessionStore.level`(isActive 대체), `CrisisCheck.level`,
  `CrisisFilter`, `ChatOrchestrator`(crisisText(category, level) + 문구 2종). 프롬프트 3파일.
  테스트 81건 통과.

### D-021. 서비스명 확정: 앱 "엠마오" · 챗봇 "바나바"
- 날짜: 2026-07-16 (민규 결정)
- 이유: 둘 다 기독교 출처. **엠마오** = 눅 24장, 부활하신 예수가 낙심한 제자들과 함께 걸으며 말씀을
  풀어주신 길("곁에서 함께 걷는 신앙 동반자"). **바나바** = "위로의 아들"(행 4:36; 위로·권면하는
  챗봇 정체성).
- 결정: 가칭 "말씀벗" → **앱 이름 = 엠마오**, **챗봇(어시스턴트) 이름 = 바나바**로 확정.
  스토어·개발자 콘솔 앱명은 "엠마오".
- 범위: 사용자 노출(앱명·콘솔·프롬프트 내 봇 자기지칭)에만 반영. **내부 코드베이스·패키지
  `com.malssumbeot`는 그대로 유지 권장** — 루트 패키지 리네임은 고비용·무이득(내부 코드명 ≠ 제품명).
  전체 리네임 여부 최종 확정은 민규.
- 파생(별도 처리): 챗봇이 스스로를 "바나바"로 지칭하는 것은 프롬프트(master.txt 등) 수정 =
  사람 승인 항목이라 프롬프트 작업 시 반영. 성명표시권(D-016)·개인정보처리방침 문구도 앱명 반영 필요.
- 영향: 스토어/콘솔 앱명, 향후 프롬프트의 봇 자기지칭, CLAUDE.md 제품 개요.

### D-022. 소셜 로그인 방식: 토큰 검증형(방식 A) — 백엔드는 제공자 토큰을 검증만, 자체 JWT 발급
- 날짜: 2026-07-20 (민규 결정)
- 배경: MVP 클라이언트가 RN(Expo) 모바일 앱(D-002). OAuth를 (A) 토큰 검증형 vs (B) 서버 리다이렉트형
  중 선택. B는 웹 브라우저 앱용이라 네이티브 앱에선 커스텀 스킴 딥링크 리다이렉트를 억지로 끼워야 함.
- 결정: **방식 A** 채택. 흐름 = 앱이 구글/카카오 SDK로 제공자 토큰 획득 → 백엔드로 전송 →
  백엔드가 토큰 진위 검증 → `UserRepository.findByProviderAndProviderId`로 User 조회/생성(upsert) →
  **우리 자체 JWT 발급**. 이후 앱은 우리 JWT로 모든 API에 접속(제공자 토큰은 로그인 순간 1회만 사용).
  검증 방법: 구글=ID 토큰(JWT) 서명·audience 검증, 카카오=액세스 토큰으로 사용자정보 API 호출.
- 이유: (1) 모바일 네이티브 로그인 표준, (2) 백엔드에 리다이렉트/콜백 처리 없음 → 단순,
  (3) 카카오 Client Secret·Redirect URI 불필요 → 콘솔 설정 간소, (4) 사용자 비밀번호를 우리가 안 봄,
  (5) 기존 `User(provider, providerId)` 유니크 구조와 정확히 부합.
- 영향: `com.malssumbeot.auth`(신규) — 제공자별 TokenVerifier(인터페이스, 테스트는 mock), AuthService,
  JwtService, AuthController(`POST /api/auth/{provider}`), 인증 DTO. 카카오 콘솔은 Client Secret·
  Redirect URI 등록 불필요(플랫폼 키해시/번들ID는 RN 앱 생성 시). 외부 키는 사람 승인 항목이라 .env로 관리.

## 재검토 요청

(없음)

## 미결 사항 → PROGRESS.md "사람 확인 필요" 참조
